# Leadlly Metrics

Read-only Next.js dashboard for student, teacher/mentor, and institute data.

```bash
npm install
cp .env.example .env.local
# set LEADLLY_DB_URL, METRICS_USERNAME, METRICS_PASSWORD
npm run dev
```

Sign in at `/signin`. All other pages and APIs require that session.
